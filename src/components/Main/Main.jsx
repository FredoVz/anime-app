export default function Main({ children }) {
  return (
    <main className="main">
      {children}
      {/* <Box animes={animes} onSelectedAnime={handleSelectedAnime} />
      <SelectedBox selectedAnime={selectedAnime} /> */}
    </main>
  );
}
